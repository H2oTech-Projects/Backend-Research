#strategy 3

DJANGO_SECRET_KEY=your-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
DATABASE_URL=postgres://myuser:mypassword@localhost:5432/mydatabase

mpostgres:// specifies the PostgreSQL database engine.
myuser is the database username.
mypassword is the database password.
localhost is the database host (use the server's IP or hostname in production).
5432 is the default port for PostgreSQL.
mydatabase is the name of the database.